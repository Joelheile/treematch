Please help me fix the Jest test errors in my Next.js TypeScript project that uses Supabase for authentication.

PROJECT CONTEXT:
- Next.js project with TypeScript
- Uses Supabase for authentication
- Testing with Jest and React Testing Library
- Main test failures are in __tests__/components/AuthProvider.test.tsx

TEST FAILURES TO FIX:

1. INVALID CREDENTIALS ERROR:
   - Test: "should throw error when signInWithPassword fails"
   - Issue: Test is throwing "Invalid credentials" error instead of the expected behavior
   - Need to properly mock the signIn failure scenario

2. SESSION STATE MISMATCH:
   - Test: Auth state changes should update user and session state
   - Issue: Test expects "has-session" but receives "no-session"
   - Need to properly simulate auth state change to show session exists

3. REACT ACT() WARNINGS:
   - Multiple warnings: "An update to [Component] inside a test was not wrapped in act(...)"
   - Affects: AuthProvider, StudentCard, LoginPage components
   - Need to wrap all React state updates in act() function
   - Common state updates causing warnings: setLoading, setError, setEmail, setPassword, popup toggles

REQUIREMENTS:
- Fix the "Invalid credentials" error by ensuring signIn failure is correctly mocked
- Fix the session state test to properly simulate auth state change showing "has-session"
- Wrap ALL React state updates in act() to eliminate warnings
- Ensure all asynchronous operations are properly awaited
- Maintain existing test structure and intent
- Use React Testing Library best practices
- Properly configure mocks for Supabase auth methods
- Handle async timing issues in tests
- Keep tests readable and maintainable

Please provide the corrected test code for the AuthProvider.test.tsx file that addresses all these issues while maintaining test coverage and original test meanings.

